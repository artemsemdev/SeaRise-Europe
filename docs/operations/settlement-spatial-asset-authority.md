# Settlement spatial asset authority
`prepare_spatial_asset_authority` validates pinned evidence, opens every input with no-follow descriptor traversal, and copies exact bytes privately.
Cleanup requires an effective-user-owned work root without group/other writes. `SpatialAssetPaths` is constructible path data, not an active capability. The spatial-classification stage alone issues an expiring opaque capability for DuckDB `LOAD` and `ST_Read` from these snapshots; public source coordinates are not executable capabilities. If descriptor identity cannot be established, cleanup fails closed and retains the exact randomly named `.spatial-assets-...` residue for inspection; this is not an ordinary success/no-leak guarantee. Recover only after no process is active: inspect that exact entry, then remove only that verified path—never use a glob or broad deletion. POSIX leaves hostile same-UID mutation residual; aliens fail closed; no approval is granted.
